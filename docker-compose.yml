services:
  book:
    build: 
      context: .
      dockerfile: ./Dockerfile
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes: 
      - ./Book:/app
    command: python3 manage.py runserver 0.0.0.0:8000
